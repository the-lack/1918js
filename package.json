{
  "name": "1918js",
  "version": "0.0.3",
  "license": "MIT",
  "author": "the-lack",
  "description": "typescript validation library for polish identifiers: regon, nip, pesel",

  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "works": "git clean -f -d -X && bun install --frozen-lockfile && tsc && bun test --concurrent --randomize --max-concurrency=4 --parallel --coverage",
    "test:mutation": "stryker run"
  },

  "devDependencies": {
    "@fast-check/vitest": "^0.4.1",
    "@stryker-mutator/core": "^10.0.0",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "4.1.10",
    "@vitest/ui": "4.1.10",
    "fast-check": "^4.9.0",
    "stryker-mutator-bun-runner": "^0.4.0",
    "tsdown": "^0.22.14",
    "typescript": "^7.0.0",
    "vitest": "^4.1.10",
    "1918js": "^0.0.3"
  },

  "homepage": "https://github.com/the-lack/1918js",
  "url": "https://github.com/the-lack/1918js/issues",
  "repository": {
	  "type": "git",
	  "url": "https://github.com/the-lack/1918js.git"
  },
  "keywords": [
	"regon", "nip", "pesel", "validation", 
	"poland", "polish", "value object", 
	"identifiers", "validator", "pesel validator", 
	"pesel validation", "regon validator", 
	"regon validation", "typescript", "javascript", 
	"nip validation", "nip validator"
  ]
}

